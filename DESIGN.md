# 数字江河 · V3 设计系统

## 设计意图

昼光资料室里的数据新闻：研究台账负责给出证据范围，地方现场负责让关系落地。页面像一份被打开的调查档案，阅读顺序清楚，视觉克制但不冷漠。

## 世界与材料

- 大面积冷灰纸面，局部使用雾蓝、田野绿和少量朱砂；不使用霓虹、渐变主背景、玻璃拟态或全局噪点。
- 细线、刻度、编号和路线图例构成秩序语言；路线只表达阅读或行动顺序，不伪造地理轨迹。
- 数据图采用横向比例条、100% 堆叠条、点阵重排和证据表；不使用 donut、雷达、伪趋势线或无数据网络图。
- 案例由真实新华社照片承担现场感，图片自然比例、无滤镜、无 hover 放大。
- 普通内容使用直角或轻微 2px 圆角；状态标签可用胶囊，节点使用圆形。没有厚重产品卡片。

## 场景光线

评委和普通读者在白天或明亮室内阅读一篇长篇 H5。浅底保证纸面信息可持续阅读，深墨蓝只用于少量“档案页”与反差段落，避免整站压成黑色工作台。

## Tokens

```text
paper       #F3F4F1
paper-2     #E7EBEA
paper-3     #DCE4E5
ink         #172A3A
ink-soft    #405564
mist        #7E99AA
accent      #B84F3E
field       #667D58
line        #CBD4D7
night       #213746
```

- 中文正文优先系统无衬线，标题使用宋体 / 思源宋体类衬线。
- 等宽字体只用于数字、日期、来源编号和状态代码。
- 正文最宽约 68ch；正文行高 1.8；核心控件最小触摸高度 44px。
- 文字和背景维持 WCAG AA；状态不靠颜色单独表达。

## 结构

- `SceneShell` 统一 section、锚点、标题、引言和 responsive container。
- `VizFigure` 统一数据图证据元数据和 fallback 表格。
- `EvidenceTable` 桌面为表格，移动端为纵向条目或自身容器横滑；body 不横向滚动。
- `ImageCard` 默认自然比例，支持 `width` / `height`、alt、caption、credit、synthetic 标记和失败占位。
- `NavBar` 桌面固定显示 7 个阅读入口与进度，移动端为 `03 / 07 · 社区` + 目录 dialog。

## 动效

只保留：77 节点分组重排、黄山步骤切换、少量 150—300ms reveal。节点过渡约 360ms；减弱动效时直接切换，任何文字和表格都不消失。删除 pointer parallax、hover 浮起、图片 zoom、粒子、气泡、路线摇摆和 finale network。

## 证据表达

所有主线数字来自 `src/data/evidence.ts`。每个百分比派生人数都显示“约”；所有政策记录带“措施 / 已报道 / 目标”文字状态；77 家只做聚合结构，不伪造逐家明细。

## 验收底线

Scene 0—7 独立可读；390 / 430 / 768 / 1024 / 1440 / 1920px 无全局横向溢出；图片有尺寸、alt 和失败占位；图表有静态表格；键盘、触摸、Escape、focus-visible 和 reduced-motion 可用。V3 已发布为公开 GitHub Pages 版本，正式比赛版仍需另行确认。
